package lesson1;

public class Stadium extends Course {
    private int length;

    public Stadium() {
        setLength(0);
    }
    public Stadium(int length) {
        setLength(length);
    }
}
